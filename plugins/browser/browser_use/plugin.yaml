name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed managed provider tool gateway. Also powers the 'managed provider Subscription' UX flow that bills usage to a managed provider subscription."
author: Omni Loop Labs
kind: backend
provides_browser_providers:
  - browser-use
