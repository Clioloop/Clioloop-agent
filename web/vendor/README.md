# Vendor Directory

This directory is intentionally empty.

The dashboard now uses the first-party `@clioloop-agent/ui` workspace package at
`packages/clioloop-ui`. Keep shared visual primitives there so web, desktop,
and installer surfaces all resolve the same Omni Loop Labs UI code.
